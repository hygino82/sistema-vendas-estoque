version: "3.9"

services:
  mysql:
    image: docker.io/library/mysql:8.0
    container_name: podman_mysql
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: 89631139
      MYSQL_DATABASE: sistemaestoque
      MYSQL_USER: hygino
      MYSQL_PASSWORD: 89631139
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data:
